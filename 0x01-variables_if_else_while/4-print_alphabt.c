#include <stdio.h>
/**
*main-main prints the alphabet in lowercase
*Description:followed by a new line
*except q and e.
*Return :Always 0 (success)
*/
int main(void)
{
	char ch = ' a ';

	while (ch <= 'z')
	{
		if (c != 'e' && c != 'q')
		{
			putchar(ch);
		}
		 ch++;
	}
	putchar('\n');
	return (0);
}
